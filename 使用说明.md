# 激情解说 TTS 部署说明

> 需求：根据解说词文字，用激情洋溢的声音进行解说（类似足球解说/游戏解说），只要有文字就能解说。
> 方案：从 GitHub 选了 3 个**大模型（LLM 架构）语音合成项目**，全部本地部署，GPU 推理（RTX 4080 16GB）。

## 一、已部署的 3 个项目

| # | 项目 | 出品方 | 用它做什么 | 状态 |
|---|------|--------|-----------|------|
| 1 | **CosyVoice 2**（0.5B） | 阿里 FunAudioLLM | 用自然语言指令控制语气，如"用极其兴奋、激动、呐喊的语气解说" | ✅ 已验证出声 |
| 2 | **IndexTTS-2** | 哔哩哔哩（B站） | 主打强情感表达：情感向量（喜/怒/哀/惧…8维）+ 情感文字描述控制 | ✅ 已验证出声 |
| 3 | **GPT-SoVITS**（v2） | RVC-Boss（12万+ star） | 参考音频音色克隆：给一段解说员录音，即可用TA的音色激情解说 | 部署中（见下文） |

三个项目都是 LLM 架构的大模型 TTS（CosyVoice2 内置 0.5B LLM 骨干；IndexTTS2 为 LLM+扩散；GPT-SoVITS 为 GPT+VITS），完全离线运行，无任何 API 收费。

## 二、目录结构

> **每个项目都是自包含的**：启动脚本、演示脚本、虚拟环境全在项目内部。把整个文件夹复制到新电脑也能直接运行（需已安装 CUDA）。

```
G:\xmgousi\jieshuoxm\
├── CosyVoice\                    # 项目1：CosyVoice 2（自包含）
│   ├── pretrained_models\CosyVoice2-0.5B\   # 模型（约4.6G，ModelScope下载）
│   ├── asset\                    # 参考音频（zero_shot_prompt.wav）
│   ├── demo_jieshuo.py           # 激情解说演示脚本
│   ├── 启动_WebUI.bat            # 启动 WebUI（端口 9600）
│   ├── 启动_演示.bat             # 运行 demo_jieshuo.py
│   └── .venv\                    # 独立环境 (Python 3.10 + torch 2.3.1 cu121)
├── index-tts\                    # 项目2：IndexTTS-2（自包含）
│   ├── checkpoints\IndexTTS-2\   # 模型（约12G，ModelScope+hf-mirror下载）
│   ├── asset\                    # 参考音频（zero_shot_prompt.wav）
│   ├── demo_jieshuo.py           # 激情解说演示脚本
│   ├── 启动_WebUI.bat            # 启动 WebUI（端口 9601）
│   ├── 启动_演示.bat             # 运行 demo_jieshuo.py
│   └── .venv\                    # 独立环境 (Python 3.11 + torch 2.8.0 cu128)
├── GPT-SoVITS\                   # 项目3：GPT-SoVITS（自包含）
│   ├── GPT_SoVITS\pretrained_models\        # 模型（约1.1G，hf-mirror下载）
│   ├── demo_jieshuo.py           # 激情解说演示脚本
│   ├── 启动_WebUI.bat            # 启动推理 WebUI（端口 9872）
│   ├── 启动_API.bat              # 启动 HTTP API（端口 9885）
│   ├── 启动_演示.bat             # 运行 demo_jieshuo.py
│   └── .venv\                    # 独立环境 (Python 3.10 + torch 2.5.1 cu124)
├── 演示音频\                     # 各项目生成的激情解说音频（根目录结构下）
├── _scripts\                     # 安装日志、解说词样例等
├── 启动_CosyVoice2_WebUI.bat     # 【快捷方式】→ 调用 CosyVoice\启动_WebUI.bat
├── 启动_IndexTTS2_WebUI.bat      # 【快捷方式】→ 调用 index-tts\启动_WebUI.bat
├── 启动_GPT-SoVITS_API.bat       # 【快捷方式】→ 调用 GPT-SoVITS\启动_API.bat
└── 启动_GPT-SoVITS_WebUI.bat     # 【快捷方式】→ 调用 GPT-SoVITS\启动_WebUI.bat
```

## 三、怎么用（最简）

### 方式1：从根目录启动（快捷方式）
双击根目录的 `启动_*.bat`，效果与进入项目内部启动完全一致。

### 方式2：进入项目内部启动（推荐，独立运行）
每个项目都可以单独复制到新电脑，进入项目目录后双击对应的 bat：

| 项目 | 启动 WebUI | 启动 API | 运行演示 |
|------|-----------|---------|---------|
| **CosyVoice** | `启动_WebUI.bat` → http://127.0.0.1:9600 | — | `启动_演示.bat` |
| **IndexTTS-2** | `启动_WebUI.bat` → http://127.0.0.1:9601 | — | `启动_演示.bat` |
| **GPT-SoVITS** | `启动_WebUI.bat` → http://127.0.0.1:9872 | `启动_API.bat` → http://127.0.0.1:9885 | `启动_演示.bat` |

### 方式3：批量/程序调用
改各项目里的 `demo_jieshuo.py` 中文本后，双击 `启动_演示.bat` 或命令行运行 `.venv\Scripts\python demo_jieshuo.py`。

> 注意：GPT-SoVITS 的演示脚本依赖 API 服务，需先启动 `启动_API.bat`。
3. 首次启动加载模型约 1~4 分钟，属正常（模型要从机械盘读入显存）。

### 显存提示（重要）
16GB 显存同时只跑 **一个** 项目的模型。跑第二个项目前先关闭前一个（关闭 bat 窗口即可释放）。

## 四、各项目"激情解说"控制方法

### CosyVoice 2 —— 指令控制（instruct2 模式）
用一句话描述想要的情绪，核心参数 `instruct_text`：
```python
model.inference_instruct2(
    "漂亮！球进了！这是一记世界波！……",          # 解说词
    "你是一位无比激情的足球解说员，请用极其兴奋、激动、呐喊的语气解说",  # 语气指令
    "asset/zero_shot_prompt.wav")               # 音色参考（可换任意说话人录音）
```

### IndexTTS-2 —— 情感向量 / 情感文字
```python
tts.infer(spk_audio_prompt=参考音频, text=解说词, output_path=输出.wav,
          use_emo_text=True, emo_text="极度激动兴奋，热血呐喊")   # 方式1：文字描述
          # 或 emo_vector=[喜,怒,哀,惧,厌恶,低落,惊喜,平静]        # 方式2：8维向量，如 [0.9,0,0,0,0,0,0.85,0]
```
（内置 QwenEmotion 模型自动把情感文字转成向量，演示中"激动兴奋呐喊"被解析为 happy=0.9）

### GPT-SoVITS —— 音色克隆
给一段 3~10 秒目标解说员的录音（`ref_audio_path`）+ 对应文字（`prompt_text`），输出即用该音色朗读新解说词：
```python
requests.post("http://127.0.0.1:9885/tts", json={
    "text": "新的解说词……", "text_lang": "zh",
    "ref_audio_path": "参考录音.wav", "prompt_text": "参考录音里说的话", "prompt_lang": "zh"})
```

## 五、演示音频

`演示音频\` 目录（由 `demo_jieshuo.py` 生成，可直接试听对比三个引擎的激情解说效果）：
- `CosyVoice2\01_足球解说_instruct2_激动呐喊.wav` 等 3 条
- `IndexTTS-2\00_冒烟测试.wav` 等（demo_jieshuo.py 可再生成 2 条完整版）
- `GPT-SoVITS\`（部署完成后运行 demo_jieshuo.py 生成）

## 六、网络与依赖说明（本次部署踩坑记录）

- GitHub 直连不通：git 已配置 `ghfast.top` 代理；pip 用阿里云/清华镜像；模型走 **ModelScope**（CosyVoice2、IndexTTS-2 官方仓库）与 **hf-mirror.com**（GPT-SoVITS）。
- Windows 上无需 conda：CosyVoice 与 IndexTTS-2 均已改用纯 Python 的 `wetext` 做文本正则化。
- GPT-SoVITS 依赖里的 `--no-binary=opencc` 已去除（用官方 wheel）；openai-whisper、pyopenjtalk 两个老包需要本地构建（setuptools<81 + cmake 3.31.6 + venv 内编译，脚本已处理）。

## 七、常见问题

- **启动报 CUDA out of memory**：先关掉另一个项目的进程（`taskkill` 或关窗口），再启动。
- **首次合成很慢**：第一次推理包含 CUDA kernel 预热，属正常；之后 CosyVoice2 约 1.3x 实时，IndexTTS-2 更快。
- **想换音色**：把 `demo_jieshuo.py` 或 WebUI 里的参考音频换成任意 3~10 秒清晰人声即可（IndexTTS-2/GPT-SoVITS 零样本克隆）。
